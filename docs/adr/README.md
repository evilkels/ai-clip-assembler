# Architecture Decision Records

One file per decision, numbered (`0001-<slug>.md`). None recorded yet —
key decisions so far live in `docs/plans/project-folder-model.md` (project =
folder on disk) and `docs/ARCHITECTURE.md`.
